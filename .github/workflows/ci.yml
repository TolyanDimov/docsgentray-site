name: CI
on: [push, pull_request]

jobs:
  lint-test-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install -r backend/requirements.txt
      - run: pytest tests -q --cov=backend --cov-report=term-missing
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: cd frontend && npm ci && npm run build
      - run: docker build -t docsgentray-backend ./backend
      - run: docker build -t docsgentray-frontend ./frontend
      - run: docker build -t docsgentray-bot ./telegram-bot
      - run: echo "Security scan placeholder (add Trivy/Snyk in secrets-enabled org)"
